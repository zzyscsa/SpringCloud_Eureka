eureka:
  client:
    service-url:
      defaultZone: http://localhost:8761/eureka/ #注册的目标，注册中心自己注册上去
    register-with-eureka: false #不在界面上显示
spring:
  application:
    name: eureka
server:
  port: 8761